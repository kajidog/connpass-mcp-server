version: '3.8'

services:
  connpass-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      # Persist cache data
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
